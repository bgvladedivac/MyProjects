 Sends an email when a crypto asset reaches out a specific desired value.
    The metrics are being taken by a file. The email pass security is handled by base64 encode. You can run it as a cron job.
